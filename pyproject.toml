[project]
name = "mmp"
version = "0.1.0"
description = "Mobile Measurement Platform — Python + Postgres"
requires-python = ">=3.12"
dependencies = [
    "mmp-core",
    "mmp-crypto",
    "mmp-db",
    "mmp-ingest",
    "mmp-attrib",
    "mmp-providers",
    "mmp-tracker",
    "mmp-api",
    "mmp-worker",
    "mmp-web",
]

[dependency-groups]
dev = [
    "ruff>=0.14",
    "mypy>=1.18",
    "bandit>=1.9",
    "pip-audit>=2.9",
    "pytest>=8.4",
    "pytest-asyncio>=1.2",
    "httpx>=0.28",
    "anyio>=4.11",
]

[tool.uv.workspace]
members = ["packages/*", "services/*"]

[tool.uv.sources]
mmp-core = { workspace = true }
mmp-crypto = { workspace = true }
mmp-db = { workspace = true }
mmp-ingest = { workspace = true }
mmp-attrib = { workspace = true }
mmp-providers = { workspace = true }
mmp-tracker = { workspace = true }
mmp-api = { workspace = true }
mmp-worker = { workspace = true }
mmp-web = { workspace = true }

# ---------------------------------------------------------------- lint
[tool.ruff]
line-length = 100
target-version = "py312"
src = ["packages", "services"]

[tool.ruff.lint]
select = [
    "E", "W",    # pycodestyle
    "F",         # pyflakes
    "I",         # isort
    "UP",        # pyupgrade
    "B",         # bugbear
    "S",         # bandit-in-ruff
    "ASYNC",     # async correctness
    "C4", "SIM", # comprehensions / simplify
    "RUF",
]
ignore = ["S101"]  # assert is fine in tests; pytest needs it

[tool.ruff.lint.per-file-ignores]
"**/tests/**" = ["S105", "S106", "SIM117"]
# Load-testing harness: it deliberately spawns a server subprocess and touches
# the filesystem from async code. Neither is a concern in a developer tool that
# never runs in production.
"infra/load/*.py" = ["ASYNC109", "ASYNC220", "ASYNC240", "S110"]
# The migration test shells out to createdb/psql/alembic by name, exactly as a
# developer would. The executables are not attacker-controlled.
"tests/test_migrations.py" = ["S603", "S607"]

# The single most important lint rule in this repo: no f-string SQL, ever.
[tool.ruff.lint.flake8-bandit]
check-typed-exception = true

# ---------------------------------------------------------------- types
[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
disallow_any_explicit = false
plugins = []
mypy_path = "packages/mmp_core/src:packages/mmp_crypto/src:packages/mmp_db/src:packages/mmp_ingest/src:packages/mmp_attrib/src:packages/mmp_providers/src:services/tracker/src:services/api/src:services/worker/src:services/web/src"

[[tool.mypy.overrides]]
module = ["tests.*"]
disallow_untyped_defs = false

# asyncpg ships no py.typed marker. Named explicitly so that a future untyped
# dependency does not slip in under a blanket ignore_missing_imports.
[[tool.mypy.overrides]]
module = ["asyncpg.*"]
ignore_missing_imports = true

# boto3 is an optional dependency, imported lazily inside the KMS provider so
# that services which never wrap a credential do not carry an AWS SDK. It is
# named explicitly rather than covered by a blanket ignore.
[[tool.mypy.overrides]]
module = ["boto3.*"]
ignore_missing_imports = true

# ---------------------------------------------------------------- tests
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["packages", "services", "tests"]
python_files = ["test_*.py"]
addopts = "-q --strict-markers"
markers = [
    "slow: creates a database and runs the full migration chain",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true
