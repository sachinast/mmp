"""Provider adapters.

One file per integration. An adapter declares what it accepts, maps our event
names to the provider's, and builds a request — nothing else. See
``mmp_providers.base`` for why the contract is that narrow.
"""
